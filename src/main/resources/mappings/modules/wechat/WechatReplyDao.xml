<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzkans.crm.modules.wechat.dao.WechatReplyDao">

    <sql id="wechatNewReplyColumns">
		a.id AS "id",
		a.un_open AS "unOpen",
		a.reply_type AS "replyType",
		a.content_type AS "contentType",
		a.key_type AS "keyType",
		a.keywords AS "keywords",
		a.remarks AS "remarks",
		a.reply_desc AS "replyDesc",
		a.reply_way AS "replyWay",
		a.create_date AS "createDate",
		a.update_date AS "updateDate",
		a.creator AS "creator",
		a.updator AS "updator",
		a.deleted AS "deleted",
		a.wechat_id AS "wechatId"
	</sql>

    <sql id="wechatNewReplyJoins">
    </sql>

    <select id="get" resultType="WechatReply">
        SELECT
        <include refid="wechatNewReplyColumns"/>
        FROM t_wecha_new_reply a
        <include refid="wechatNewReplyJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="WechatReply">
        SELECT
        <include refid="wechatNewReplyColumns"/>
        FROM t_wecha_new_reply a
        <include refid="wechatNewReplyJoins"/>
        <where>
            a.deleted = 0 and a.wechat_id =#{wechatId}
            <if test="id != null and id != ''">
                AND a.id = #{id}
            </if>
            <if test="unOpen != null and unOpen != ''">
                AND a.un_open = #{unOpen}
            </if>
            <if test="replyType != null and replyType != ''">
                AND a.reply_type = #{replyType}
            </if>
            <if test="contentType != null and contentType != ''">
                AND a.content_type = #{contentType}
            </if>
            <if test="keyType != null and keyType != ''">
                AND a.key_type = #{keyType}
            </if>
            <if test="keywords != null and keywords != ''">
                AND a.keywords = #{keywords}
            </if>
            <if test="replyWay != null and replyWay != ''">
                AND a.reply_way = #{replyWay}
            </if>
            <if test="beginCreateDate != null and endCreateDate != null and beginCreateDate != '' and endCreateDate != ''">
                AND a.create_date BETWEEN #{beginCreateDate} AND #{endCreateDate}
            </if>
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <select id="findAllList" resultType="WechatReply">
        SELECT
        <include refid="wechatNewReplyColumns"/>
        FROM t_wecha_new_reply a
        <include refid="wechatNewReplyJoins"/>
        <where>

        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <insert id="saveReply" parameterType="com.hzkans.crm.modules.wechat.entity.WechatReply">
		INSERT INTO t_wecha_new_reply(
			un_open,
			reply_type,
			content_type,
			key_type,
			keywords,
			remarks,
			reply_desc,
			reply_way,
			create_date,
			update_date,
			creator,
			updator,
			deleted,
			wechat_id
		) VALUES (
			#{unOpen},
			#{replyType},
			#{contentType},
			#{keyType},
			#{keywords},
			#{remarks},
			#{replyDesc},
			#{replyWay},
			now(),
			now(),
			#{creator},
			#{updator},
			0,
			#{wechatId}
		)
	</insert>

    <update id="updateReply" parameterType="com.hzkans.crm.modules.wechat.entity.WechatReply">
        UPDATE t_wecha_new_reply
        <set>
            <if test="unOpen != null">
                un_open = #{unOpen},
            </if>
            <if test="replyType != null">
                reply_type = #{replyType},
            </if>
            <if test="contentType != null">
                content_type = #{contentType},
            </if>
            <if test="keyType != null">
                key_type = #{keyType},
            </if>
            <if test="keywords != null">
                keywords = #{keywords},
            </if>
            <if test="remarks != null">
                remarks = #{remarks},
            </if>
            <if test="replyDesc != null">
                reply_desc = #{replyDesc},
            </if>
            <if test="replyWay != null">
                reply_way = #{replyWay},
            </if>
            <if test="updator != null">
                updator = #{updator},
            </if>
            update_date = now()
        </set>
        where id = #{id} and wechat_id = #{wechatId}
    </update>

    <update id="delete">
		UPDATE t_wecha_new_reply SET
		deleted = 1
		WHERE id = #{id}
	</update>

    <select id="getReplyByKeywords" parameterType="java.lang.String"
            resultType="com.hzkans.crm.modules.wechat.entity.WechatReply">
        SELECT
        <include refid="wechatNewReplyColumns"/>
        FROM t_wecha_new_reply a
        WHERE a.keywords = #{keywords} and a.deleted = 0
    </select>
</mapper>